export const PHONE_PATTERN = /^\+?\d{7,10}$/;

export const SCHOOL_CODE_PATTERN = /^[A-Z]{1,5}$/;

export const STUDENT_CODE_PATTERN = /.+/;

export const RESPONSE_CODE = {
    FAIL: "fail",
    SUCCESS: "success"
}

export const ACTION = {
    CREATE: "CREATE",
    UPDATE: "UPDATE",
    DELETE: "DELETE"
}